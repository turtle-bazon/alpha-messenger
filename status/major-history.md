1. Заложена первичная документация продукта в doc: architecture.md (транспорт WebSocket+REST — действия по REST, поток событий по WS с resume через hello/lastSeq; общий канал событий, push как отдельный канал FCM/UnifiedPush, PostgreSQL, стек Fastify+@fastify/websocket+pg, упрощённая регистрация username+password только по инвайт-коду (без ролей; коды генерит скрипт npm run invite), отказ от кэша сообщений на клиенте в v1), api.md (черновик контракта REST + WS), ui.md (ориентир на десктопный Telegram, центрирование с max-width).
2. Поднят каркас сервера на TypeScript (Fastify + node-postgres) в src/server с раннером миграций и эндпоинтом /health; собран run/docker-compose.yaml (сервис + PostgreSQL). Проверено: стек поднимается, /health отдаёт ok с обращением к БД.
3. Заведена первичная схема БД (migrations/0001_init.sql): accounts, devices, sessions, chats, chat_members, messages, push_subscriptions, events (outbox). Идентичность сущностей — uuid; порядок сообщений (message_id) и курсор потока событий (seq) — глобальные IDENTITY-последовательности. Проверено: миграция применяется, все таблицы создаются.
4. Реализована регистрация по инвайт-кодам (migrations/0002_invites.sql, скрипт npm run invite, порт Postgres опубликован на 127.0.0.1 для генерации с хоста) и аутентификация: POST /auth/register, /auth/login (argon2id, сессии на пару аккаунт+устройство, авто-регистрация устройства с событиями device.added/auth.attempt в outbox), POST /devices, GET /me. Покрыто функциональным тестом test/auth.test.ts; проверено на реальном контейнере.
5. Реализованы чаты: GET /chats, POST /chats (direct с дедупликацией, group), GET /chats/{chatId} с проверкой участия. Общий вид чата (loadChat) с participants/lastMessage/unreadCount; при создании эмитится chat.created. Покрыто тестом test/chats.test.ts.
6. Реализованы сообщения: отправка (идемпотентность по clientMessageId, ciphertext base64↔bytea), история с пагинацией по messageId, редактирование и удаление автором, отметка прочтения. Все операции эмитят message.* в outbox; доступ только участнику. Покрыто test/messages.test.ts.
7. Реализован WebSocket /ws (@fastify/websocket): hello+lastSeq делает replay из outbox, живая доставка через pg_notify→LISTEN→in-process хаб (catch-up по seq), приём typing (транзиентно) и read (общий markRead). Тот же hello/replay закрывает досинхронизацию после офлайна/пуша. Общие хелперы вынесены в chat-helpers.ts. Покрыто test/ws.test.ts.
8. Реализованы push-подписки (POST/DELETE /push/subscriptions, идемпотентность по (deviceId,endpoint), привязка к своему устройству) и заглушка wake-up: notify в /ws при отсутствии живого WS у получателя будит устройство пушем без содержимого (точка интеграции FCM/UnifiedPush инкапсулирована в push.ts, миграция 0003). Раннер миграций сериализован advisory-локом (защита от гонки конкурентных прогонов на свежей БД). Покрыто test/push.test.ts.
9. Выбран стек клиента и тестов UI (зафиксировано в doc/architecture.md): TypeScript + React + Vite для web-клиента (его обернут desktop/android), сквозные UI-тесты на Playwright против реального docker-стека (ключевой довод — мультиконтекст для проверки живой WS-доставки между аккаунтами). Поднят каркас web-клиента в src/web_client (центрирование с max-width по ui.md), настроен Playwright + сидинг инвайтов в БД; smoke-сценарий зелёный.
10. Реализованы транспортный слой клиента (REST-обёртка на fetch с ApiError, WS-клиент с reconnect/backoff и resume через hello/lastSeq + диспетчер событий, session.ts для токена/deviceId) и экраны аутентификации (регистрация по инвайт-ссылке, логин, HomeScreen-заглушка). На сервер добавлен @fastify/cors (bearer-токен, origin: true). Сквозной сценарий регистрация→выход→вход (e2e/auth.spec.ts) зелёный против реального стека.
11. Реализованы чаты и переписка в клиенте: двухколоночная раскладка Telegram, список чатов (создание direct по username, превью/непрочитанные), область переписки с историей и ленивой подгрузкой, оптимистичной отправкой по clientMessageId и живым приёмом message.new по WS. Состояние чатов и WS-соединение подняты в HomeScreen; live chat.created/message.new обновляют список. Покрыто e2e/chats.spec.ts и двухконтекстным e2e/messages.spec.ts (живая доставка A↔B).
12. Добавлены редактирование/удаление своих сообщений, отметка прочтения (статус ✓/✓✓ по message.read) и индикатор «печатает…» (typing с throttle и авто-скрытием) — всё вживую по WS между пользователями. Покрыто e2e/interactions.spec.ts.
13. В поток событий введена граница реплея: сервер после hello-выгрузки истории шлёт управляющий маркер `synced` (без `seq`), клиент по нему отделяет «уже было» от live (WsClient.isLive()). На этом построены уведомления уровня аккаунта в UI (новый вход/новое устройство по auth.attempt+device.added, тост с авто-скрытием, свои события — по deviceId) и устранено задвоение счётчиков непрочитанного на холодном старте. Закреплён принцип: состояние — из REST, события — для live и догона пропуска. Покрыто e2e/account-events.spec.ts.
14. Введён конверт содержимого сообщения (util/content.ts: text|image поверх encodeText с фолбэком на легаси-текст) и отправка inline-изображений: кнопка 📎, простой редактор перед отправкой (превью/поворот/подпись), ужатие под потолок ≤128 КБ итогового ciphertext (downscale+JPEG). Картинка едет прямо в теле, без медиа-хранилища; рендер в пузырях, превью списка «📷 Фото». Зафиксировано: inline — только малая медиа под потолком, крупное — будущая версия с content-addressed блобами. Покрыто e2e/image.spec.ts.
15. Полировка UI под десктопный Telegram: время и мета-строка в пузырях, hover-действия вне потока, цветные аватары и двухстрочные элементы списка с временем последнего сообщения, пустое состояние, hover/active. Адаптив на ширине ≤600px — одноэкранная раскладка (список ИЛИ переписка) с кнопкой «назад». Покрыто e2e/mobile.spec.ts; полный регресс набора 8/8 зелёный.
16. Сборка/запуск разнесены на два окружения в run/: prod (всё в Docker — БД, сервер и веб-клиент статикой через nginx с SPA-fallback на 5173; конечное решение, host-Node не нужен) и dev (БД+сервер в Docker, клиент через Vite/HMR). Добавлены src/web_client/Dockerfile (vite build → nginx) и nginx.conf; invite.sh положен в каждый каталог окружения (кладёт код в БД через контейнер). README переписан под dev/prod. Prod-стек проверен: nginx отдаёт /register, клиент грузится в реальном браузере без ошибок, регистрация по invite — 201.
17. Синяя «+» в списке чатов стала полноценной точкой входа «новый чат» (как compose в Telegram): кнопка открывает модалку NewChatDialog с двумя вкладками — личный чат по username и группа (название + участники, добавление по username с чипами). Раньше «+» была скрытой кнопкой submit у поля и при пустом поле молча ничего не делала, а групп в клиенте не было вовсе (хотя сервер их уже умел). В клиент добавлен createGroup, HomeScreen — onCreateGroup. Граница центрированного клиента подчёркнута: фон страницы сделан темнее области чата (--page-bg), у .app-shell — боковые рамки и тень (на мобильной ширине убираются). Покрыто e2e/groups.spec.ts; общий хелпер createDirectViaUi (открывает диалог), регресс 9/9 зелёный.
18. Все серверные REST-эндпоинты объединены под префиксом /api/ (на сервере — плагин с prefix:'/api', /ws остаётся в корне), что упростило обратный Apache-прокси до одного правила ProxyPass /api/. Заодно устранена привязка клиента к localhost:3000 за прокси: в прод-сборке VITE_API_URL='' даёт относительные URL (запросы на тот же origin — CORS не возникает, образ переносим), wsUrl() при пустом адресе берёт схему/хост из window.location; build-arg проброшен пустым в CI, а в client Dockerfile дефолт localhost:3000 сохраняет локальную run/prod-сборку. Покрыто прогоном серверных тестов 8/8 и e2e-регрессом 9/9.
19. Участники чата в заголовке и окно состава (как в Telegram). В БД заведён создатель чата (миграция 0004: chats.created_by, проставляется при создании; loadChat отдаёт createdBy). Онлайн — бинарный presence из in-process WS-хаба: isOnline + рассылка транзиентного события presence со-участникам на переходах online↔offline, снимок через GET /api/presence (сид клиента после реплея; клиент дополнительно пересеивает при chat.created — двое уже-онлайн при создании чата событий друг о друге не получают). Эндпоинты GET /api/chats/{id}/members (состав + online + createdBy) и DELETE /api/chats/{id}/members/{userId} (право только у создателя, нельзя удалить создателя/не-группу; событие chat.member_removed оставшимся и удалённому). Клиент: подзаголовок в заголовке (группа — «N участников, M в сети»; direct — статус собеседника), клик по заголовку группы открывает MembersDialog (список с онлайн-индикатором, пометкой создателя, кнопками удаления у создателя); HomeScreen держит множество онлайна и обрабатывает member_removed (удалённый убирает чат). Покрыто серверным test/members.test.ts и e2e/members.spec.ts (presence в заголовке двухконтекстно + отдельный сценарий окна с удалением). Регресс: сервер 10/10, e2e 12/12.

20. Починены устойчивость статусов и счётчиков при свёртывании/восстановлении окна (переоткрытие чата, reconnect). Статус ✓✓ больше не деградирует в ✓: позиция прочтения собеседником сеется из серверного peerReadUpTo (loadChat) и поддерживается live-событиями message.read (в т.ч. в HomeScreen для закрытых чатов), а не живёт только в эфемерном состоянии. Устранён двойной счёт непрочитанного на холодном старте: ws.isLive() в обработчике message.new фиксируется в момент приёма, а не внутри отложенного setChats (иначе пришедший позже 'synced' выдавал реплей за live). WsClient укреплён против гонок вытесненного сокета (detach + guard this.ws !== ws). Мелочь по UI: мета-строка пузыря (время/статус) больше не переносится посимвольно. Покрыто e2e two-clients.spec.ts и interactions.spec.ts; регресс 14/14.

21. Добавление участников в группу создателем (как в Telegram; удаление уже было — п.19). Эндпоинт POST /api/chats/{id}/members с телом { username }: право только у создателя, лишь для группы, нельзя добавить уже состоящего (409). Новому участнику пишется chat.created (подтягивает чат в список), уже состоящим — новое событие chat.member_added (обновляют состав/счётчик участников; HomeScreen перечитывает чат из REST и пересеивает presence). Клиент: в MembersDialog у создателя появилась форма «Добавить по username» (под списком). Покрыто серверным test/members.test.ts (право/группа/дубль/несуществующий) и e2e members.spec.ts (двухконтекстный сценарий: создатель добавляет живого клиента — у него чат появляется вживую, у создателя растёт счётчик; не-создатель формы не видит). Регресс: сервер 11/11, e2e 15/15.

22. Пакет правок known_issues №№2,3,4,8. (8) Resume потока между сессиями: lastSeq сохраняется в localStorage per-account (session.ts getLastSeq/setLastSeq, WsClient.onSeqAdvance, сид/запись в HomeScreen) — после reload реплеится только пропущенное, история не выдаётся за live. (3) Глазик показа/скрытия пароля на входе/регистрации (общий PasswordInput, SVG eye/eye-off). (4) Участников группы выбирают из знакомых пользователей (собеседники личных чатов, без себя), а не свободным вводом username: ChatList собирает кандидатов из direct-чатов, NewChatDialog — поиск + список с чипами, у новичка без чатов подсказка. (2) Задокументирован деплой за Apache (doc/deploy.md): обязательные LoadModule включаются на стороне установки, плейсхолдер <app-host>. Покрыто новыми e2e reconnect.spec.ts и проверками в auth/groups.spec.ts; helper групп в members.spec.ts переведён на новый флоу. Регресс e2e 18/18.

23. Визуальная полировка UI до уровня современного мессенджера (known_issues №3), ориентир — десктопный Telegram. Подход: меняли собственные стили, без импорта чужого CSS; у Telegram взяли только эталонные значения (палитра, метрики). Палитра целиком вынесена в CSS-переменные (:root) — единая точка темы. Сайдбар: тень+линия отделяют от области чата, hover/active с переходом, поле поиска с иконкой-лупой и локальной фильтрацией списка, аватары с тенью. Переписка: мягкий градиент+точечный узор фона (чистый CSS), пузыри с радиусом/тенью/градиентом, хвостик у последнего в группе (псевдоэлемент, без SVG), группировка подряд идущих сообщений автора, разделители дат, синий ✓✓. Композер: округлённое поле, круглая кнопка-самолётик с анимацией появления при наличии текста, анимированные точки «печатает». Иконки — единый набор inline-SVG на currentColor (util/icons.tsx); эмодзи не используем из-за разнобоя между ОС. Тёмная тема ([data-theme='dark']) с переключателем в шапке (util/theme.ts: localStorage + дефолт по prefers-color-scheme). Аватары оставлены круглыми (как в Telegram Desktop), вопреки формулировке задачи про «скруглённый квадрат». Покрыто новым e2e ui-polish.spec.ts; регресс e2e 19/19.

24. Починено «мигание» списка чатов на логине (known_issues №4): реплей outbox перерисовывал список десятки раз и дёргал getChat/getPresence на каждый чат. WsClient теперь буферизует реплей и применяет его одним пакетом на маркер 'synced' (один ререндер; live включается после флаша, поэтому реплей по-прежнему не путается с live). HomeScreen коннектит WS только после загрузки списка из getChats (реплей ложится на готовый список), а точечные getChat/getPresence в обработчиках chat.created/member_added/member_removed зовутся лишь для реально отсутствующего чата или только вживую (на реплее состояние уже из REST + единый снимок presence по 'synced'). Закреплён принцип: на холодном старте состояние списка — из REST, реплей лишь догоняет, не переинициализирует. Покрыто новым e2e login-replay.spec.ts; регресс e2e 20/20.
