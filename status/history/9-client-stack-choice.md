# 9. Выбор стека клиента и UI-тестов

Выбран стек клиента и тестов UI (зафиксировано в doc/architecture.md): TypeScript + React + Vite для web-клиента (его обернут desktop/android), сквозные UI-тесты на Playwright против реального docker-стека (ключевой довод — мультиконтекст для проверки живой WS-доставки между аккаунтами). Поднят каркас web-клиента в src/web_client (центрирование с max-width по ui.md), настроен Playwright + сидинг инвайтов в БД; smoke-сценарий зелёный.
