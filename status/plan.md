1. Зафиксировать в doc ключевые решения: транспорт (WebSocket+REST), общий канал событий, push как отдельный канал, PostgreSQL, отказ от кэша сообщений на клиенте в v1 — СДЕЛАНО (doc/architecture.md, doc/api.md).
2. Поднять каркас сервера на TypeScript в src/server и собрать docker-compose в run (сервис + PostgreSQL).
3. Завести схему БД — СДЕЛАНО (migrations/0001_init.sql): accounts, devices, sessions, chats, chat_members, messages (ciphertext bytea + метаданные, message_id как IDENTITY), push_subscriptions, events (outbox со сквозным seq как IDENTITY). messageId и seq — глобальные IDENTITY-последовательности.
4. Реализовать аутентификацию и устройства — СДЕЛАНО: регистрация username+password (argon2id) только по инвайт-коду (invites/0002, npm run invite), POST /auth/register (проверка+гашение кода, авто-логин), POST /auth/login (сессия на пару аккаунт+устройство, авто-регистрация незнакомого устройства с эмитом device.added/auth.attempt в outbox), POST /devices, GET /me. Покрыто функциональным тестом (test/auth.test.ts), проверено на реальном контейнере.
5. Реализовать чаты — СДЕЛАНО: GET /chats (список с lastMessage/unreadCount, общий вид loadChat), POST /chats (direct с дедупликацией и проверками self/404, group с авто-добавлением создателя и эмитом chat.created всем участникам), GET /chats/{chatId} (только участнику). Покрыто тестом test/chats.test.ts.
6. Реализовать сообщения — СДЕЛАНО: POST /chats/{id}/messages (идемпотентность по clientMessageId, base64↔bytea, fan-out message.new, bump updatedAt), GET .../messages (пагинация по messageId, limit≤100), PATCH /messages/{id} (автор, message.edited), DELETE /messages/{id} (автор, стирание содержимого, message.deleted, идемпотентно), POST .../read (маркер вперёд, message.read). Доступ только участнику. Покрыто test/messages.test.ts.
7. Реализовать WebSocket GET /ws — СДЕЛАНО (@fastify/websocket): hello с lastSeq и replay из outbox; живой fan-out через pg_notify в emitEvent + отдельный LISTEN-клиент + in-process хаб (catch-up по seq); приём typing (транзиентно, мимо outbox) и read (через общий markRead). Покрыто test/ws.test.ts.
8. Досинхронизация после офлайна/пуша — СДЕЛАНО тем же hello/replay (отдельный REST since не нужен).
9. Реализовать push-подписки — СДЕЛАНО: POST /push/subscriptions (только своё устройство, идемпотентно по (deviceId,endpoint), 400/404), DELETE /push/subscriptions/{id} (только своя, идемпотентно). Заглушка sendWakeUp(userId) повешена на notify в /ws: если у получателя события нет живого WS — будим пушем без содержимого (точка интеграции FCM/UnifiedPush инкапсулирована в push.ts). Покрыто test/push.test.ts.
10. Написать минимальные функциональные тесты на каждый endpoint — СДЕЛАНО по ходу: auth/chats/messages/ws/push покрыты функциональными тестами (8 тестов, прогон зелёный на реальной БД).
11. Поднять каркас web-клиента — СДЕЛАНО: Vite + React + TS в src/web_client (dev-сервер, статическая прод-сборка, центрирование с max-width по ui.md). Playwright настроен (webServer поднимает vite, baseURL 5173), хелпер сидинга инвайтов в БД (e2e/helpers/db.ts). Smoke-сценарий «приложение открывается» зелёный.
12. Транспортный слой клиента — СДЕЛАНО: типизированный REST-клиент на fetch (ApiError со статусом, хранение токена/deviceId в session.ts) и WS-клиент (reconnect с backoff, resume через hello/lastSeq, диспетчер событий по type + onAny, typing/read). На сервер добавлен @fastify/cors (origin: true, bearer-токен без куки — обёртки кросс-ориджинны). Проверяется живьём сценарием п.13.
13. Экраны аутентификации — СДЕЛАНО: RegisterScreen (по инвайт-ссылке /register?invite=CODE, ошибки 400/409) и LoginScreen (401), deviceId генерится/хранится локально, простая маршрутизация по location, HomeScreen-заглушка после входа (username из /me, выход). Сценарий e2e/auth.spec.ts (регистрация→выход→повторный вход) зелёный против реального стека.
14. Список чатов — СДЕЛАНО: двухколоночная раскладка (сайдбар + переписка), ChatList с GET /chats, превью lastMessage (base64→текст через util/text.ts), бейдж unreadCount, имя чата по chatTitle (username собеседника для direct), создание direct по username с upsert/выбором и ошибками 404/400. Сценарий e2e/chats.spec.ts (создание чата + ошибка «не найден») зелёный.
15. Область переписки — СДЕЛАНО: Conversation с историей (GET messages, реверс DESC→ASC), ленивой подгрузкой вверх (before/limit с сохранением позиции прокрутки), пузырями (свои/чужие), оптимистичной отправкой по clientMessageId с дедупликацией WS-эха, приёмом message.new по WS. WS-клиент живёт в HomeScreen, состояние чатов поднято туда же (live chat.created/message.new обновляют список: превью/порядок/непрочитанные). Сценарий e2e/messages.spec.ts (два browser-context, живая доставка A↔B) зелёный. Прим.: пагинация вглубь реализована, отдельным сценарием пока не покрыта.
16. Редактирование/удаление/read/typing — СДЕЛАНО: edit/delete своих сообщений (действия по ховеру, оптимистично + подтверждение message.edited/deleted), отметка прочтения (авто-read до последнего сообщения в открытом чате, статус ✓/✓✓ на своих по message.read), индикатор «печатает…» (throttle на отправку typing, авто-скрытие через 6с). Превью списка тоже обновляется на edit/delete. Покрыто e2e/interactions.spec.ts (два пользователя: edit/delete/read/typing вживую).
17. События уровня аккаунта в UI — СДЕЛАНО: уведомления безопасности (новый вход/новое устройство по auth.attempt + device.added) из общего потока событий, всплывающим тостом с авто-скрытием. По ходу решена корневая проблема реплея: сервер после hello-выгрузки шлёт управляющий маркер `synced`, клиент отделяет историю от live (WsClient.isLive()) — уведомления только о живых событиях, плюс убрано задвоение непрочитанного на холодном старте. Свои события отсекаются по deviceId. Покрыто e2e/account-events.spec.ts (вход тем же аккаунтом со второго устройства → тост у открытого клиента; у только что подключившегося — тишины).
18. Редактор изображений (простой вариант) — СДЕЛАНО: введён конверт содержимого сообщения (util/content.ts, t:'text'|'image' с фолбэком на легаси-текст); прикрепление картинки (кнопка 📎 в композере), простой редактор перед отправкой (ImageEditor: превью, поворот 90°, подпись; кроп/разметка отложены). Картинка ужимается под потолок (util/image.ts: downscale + JPEG, ≤128 КБ итогового ciphertext) и едет inline в теле сообщения, без медиа-хранилища. Рендер картинок в пузырях, превью списка «📷 Фото». Сценарий e2e/image.spec.ts (A открывает редактор и шлёт картинку → B получает её вживую по WS + превью «📷»). Принято решение: inline только для малой медиа под потолком, крупные фото/видео — будущая версия с content-addressed блобами.
19. Полировка под десктопный Telegram — СДЕЛАНО: время в пузыре + мета-строка (ред./время/статус), действия над сообщением всплывают вне потока (без сдвига), цветные аватары с инициалом и двухстрочные элементы списка (заголовок+время / превью+непрочитанные), время последнего сообщения, пустое состояние пилюлей, hover/active акцентных кнопок. Адаптив: на ширине ≤600px один экран за раз (список ИЛИ переписка) с кнопкой «назад» (home--chat-open). Адаптивная логика покрыта e2e/mobile.spec.ts; визуальная часть — сборкой (тестами не проверяется).
20. Регресс всех Playwright-сценариев — СДЕЛАНО: полный прогон 8/8 зелёный (smoke, auth, chats, messages, interactions, account-events, image, mobile) против реального docker-стека.
