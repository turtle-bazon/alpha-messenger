Эта директория содержит код мессенджера - клиентскую и серверную часть, с собственным протоколом обмена сообщениями (поверх http).

## Важно
При чтении и применении этих инструкций (а также в работе над этим проектом) ОБЯЗАТЕЛЬНО учитывай и применяй диалектический ленинский принцип: "Истина всегда конкретна". Ни одна инструкция, ни одна теоретическая модель и ни одно правило не может учесть всего. Первичны всегда конкретные обстоятельства и практика. Отступления от этих правил допустимы, когда это уместно. При значительных отступлениях всегда обсуждай с пользователем.

Отвечать нужно всегда на русском языке, независимо от языка запроса.

## Важно для работы
**Не коммитить и не пушить в git без явного подтверждения от пользователя.** Даже если пользователь говорит «обнови репу» или «запиши задачу» — сначала сделать изменение, показать пользователю, и ждать команды «коммить» / «коммить и пушь».

## Образец (модель)
В реализации особенностей интерфейса нужно ориентироваться на десктопную версию Telegram. При выборе того или ного способа реализации фичи, если специально не указано иное, делай, как в Telegram. Но важно то, что это касается только интерфейса. Что касается всего остального - у нас полностью своя реализация.

## Структура директорий

### Менеджерская директория status
Эта директория содержит файлы для управления разработкой. Это не документация, это не то, что необходимо для работы программы, даже опосредовано. Это управление проектом. Содержимое разнесено по трём каталогам — по файлу на сущность (задача / пункт истории / план), чтобы не смешивать всё в один пухнущий файл и держать стабильные ссылки:

* `issues/` — задачи (проблемы и фичи), см. ниже.
* `history/` — история выполненных работ, см. ниже.
* `plans/` — планы работ, см. ниже.

### История: status/history/
История всех работ — по файлу на пункт. НО ТОЛЬКО САМОЕ КЛЮЧЕВОЕ И ВАЖНОЕ: подвигали кнопку вправо-влево, поменяли стили — этого тут быть не должно. Тупиковые решения (сделали, посмотрели, откатили) тоже не пишем; если фичу делали в несколько подходов — фиксируем только итог. Не плодить пункты без необходимости.

* Имя файла: `<номер>-<короткий-слаг>.md` (номер сквозной, по порядку; слаг латиницей), например `26-unread-notifications.md`.
* Внутри: первой строкой `# <номер>. <название>`, дальше тело пункта.
* Номер нового пункта = (максимальный существующий) + 1; добавляется при значимой завершённой работе.

### Планы: status/plans/
Планы работ — по файлу на план (`some-plan.md`, осмысленный слаг латиницей). Это позволяет вести несколько планов одновременно, не смешивая их. Внутри файла — заголовок `# <Название плана>` и нумерованный список пунктов плана (пункты можно помечать СДЕЛАНО по мере выполнения). Завершённый план можно оставить в каталоге как артефакт планирования.

### Задачи: status/issues/
Каждая задача (проблема/фича) — отдельный markdown-файл. Два подкаталога по статусу:

* `issues/open/` — актуальные (нерешённые) задачи.
* `issues/fixed/` — решённые задачи (с описанием реализации/решения внутри файла).

Правила:
* Имя файла стабильно и НЕ меняется за всю жизнь задачи: `#<номер>-<короткий-слаг>.md`, например `#14-notif-menu-clipping.md`. Номер — сквозной (единый для open и fixed), уникальный, не переиспользуется. Слаг — короткий латиницей, чтобы ориентироваться глазами.
* Номер новой задачи = (максимальный существующий номер среди open+fixed) + 1.
* Внутри файла: первой строкой заголовок `# #<номер> — <название>`, дальше тело задачи.
* После решения задачи файл БЕЗ ПЕРЕИМЕНОВАНИЯ переносится из `open/` в `fixed/` (`git mv`), а в тело дописывается реализация/решение. Имя и номер сохраняются — внешние ссылки на `#<номер>` остаются валидными.
* Наличие задачи в `open/` не означает необходимости немедленного решения, но к этому списку нужно обращаться при анализе работы программы.
* Внимание: имя файла начинается с `#`, в shell его нужно брать в кавычки (`cat 'status/issues/open/#18-live-draft.md'`); инструменты Read/Write/Glob работают без оговорок.


### Директория с документацией
doc - директория, содержащая документацию. Тут только то, что относится к разрабатываемому продукту, а не к управлению процессом.


### Директория с запускаемым образом
run - директория, содержащая запускаемый docker-composer.yaml с сервисом

### Директория с исходниками
src - директория с иходниками. Весь код - на typescript, с незначительными включениями платформенного кода при необходимости. Исходники делятся по артифактам

* server - код сервера, на typescript
* web_client - весь основной код клиента, на typescript. все другие клиенты используют этот код, так как на всех платформах будет работать данный веб-клиент в соответствующих обертках
* desktop_client - обертка web_client на electron для десктопов
* android_client - обертка web_client для android


## Команды и запуск
Краткая шпаргалка по операциям (детали и обоснования — в README и run/*).

* Сборка/typecheck сервера: `cd src/server && npm run build` (tsc).
* Сборка/typecheck клиента: `cd src/web_client && npm run build` (`tsc --noEmit && vite build`).
* Серверные тесты: `cd src/server && npm test`. Нужен Postgres на localhost:5432 (поднимается стеком run/dev); миграции тесты применяют сами. По умолчанию `DATABASE_URL=postgres://alpha:alpha@localhost:5432/alpha`.
* E2e клиента: `cd src/web_client && npx playwright test` (он же `npm run test:e2e`). Требует запущенный сервер на :3000 и БД на :5432 (стек run/dev); сам Playwright поднимает vite dev на :5173. Сидинг инвайтов идёт напрямую в БД.

Окружения в run/ (в каждом есть invite.sh для генерации инвайт-кода):
* dev — БД и сервер в Docker (порты 5432 и 3000 на хост), клиент отдельно через `npm run dev`. Поднять для тестов: `cd run/dev && docker compose up -d --build`.
* prod — всё в Docker: БД, сервер (:3100), клиент статикой через nginx (:5273). `cd run/prod && docker compose up -d --build`. Host-порты намеренно смещены (+100), чтобы prod не конфликтовал с dev — оба стека можно держать поднятыми одновременно. Сборка вшивает в клиент API-адрес localhost:3100.
* deploy — тянет готовые образы из ghcr.io, работает за Apache-прокси (apache-proxy.conf). Образы публикует CI (.github/workflows/docker-image.yml) при push в main; клиентский образ собирается с пустым VITE_API_URL (относительные URL).

Эндпоинты: REST — под префиксом /api/, поток событий — WebSocket /ws (контракт в doc/api.md).

Маршрутизация и деплой:
* nginx (внутри образа клиента, src/web_client/nginx.conf) — только раздача статики SPA c fallback на index.html (`listen 80`, без проксирования). API он не знает: в dev/prod браузер ходит на сервер напрямую по вшитому абсолютному адресу (cross-origin + CORS).
* Apache (run/deploy/apache-proxy.conf) — обратный прокси на сервере деплоя: `/ws` → :3000 (websocket-upgrade), `/api/` → :3000, всё остальное → :5173 (nginx клиента). В этом контуре клиент собран с пустым VITE_API_URL, поэтому шлёт относительные URL на тот же origin, а развязку делает Apache (CORS не возникает).
* Разница между контурами — только в адресе API у клиента: dev/prod вшивают абсолютный localhost-адрес (cross-origin), deploy — относительный (same-origin за Apache). Образ deploy переносим и не привязан к домену.

## Тестирование
* server - только минимальные, проверить каждый endpoint минимальными необходимыми тестами
* client - хорошо поработанные сценарии для тестирования web UI в браузере, начиная от регистрации и аутентификации, заканчивая редактированием сообщения и даже открытием редактора изображений
* desktop_client - тесты не нужны
* android_client - тесты не нужны

Юнит тесты писать не нужно (исключение - необходимость в более быстрых итерациях при разработке сложного решения).

## Способ разработки
В этом пункте особенно важно придерживаться принципа - "Истина всегда конкретна". Тут задан способ разработки для большинства задач, но не для всех. Мелкие задачи с простыми и очевидными фиксами - не нужно прогонять по всему описанному пайплайну. Их даже не нужно проверять без необходимости - поправил да и все.

Способ разработки большинства задач - строго итеративный, с обязательной проверкой каждого шага автоматическими функциональными тестами. Проверять в тестах нужно прежде всего конечный функционал.

1. Прочитать следующие файлы

* issues/open/ - все актуальные задачи (просмотреть список целиком)
* history/ - только последние три пункта по номеру (или больше, если сочтешь необходимым для конкретного случая)
* plans/ - только при необходимости для конкретной задачи
* issues/fixed/ - только при необходимости для конкретной задачи

2. Реализовать задачу, при необходимости итеративно, разрабатывая параллельно сценарии

3. Написать и проверить новый сценарий для задачи (только если изменения касаются проектов server и client)

4. Сделать регресс всех сценариев - при необходимости
