В проекте используется end to end enrcryption, сервер не может читать сообщения клиентов. Он хранит и отдает сообщения в зашифрованном виде.

В первой версии шифрования нет. Однако нужно исходить при разработке сервера и клиента, что сервер НЕ ЗНАЕТ текста сообщений.